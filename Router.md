these are the routes of the app

`/login` render [[login]] 
`/dashboard` render [[dashboard]] 
`/stats` render [[Stats]] 
`/config` render [[Config]] 
`/query-manager` render [[QueryManager]] 

#front 

