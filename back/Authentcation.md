an entity that represent every user that is currently connected in the app
and to prevent multiple connection of the same user
identified with
### props
token
identityJson (id,email,role =`Admin`|`User`) 
ExpiresAt


#authentication #backend